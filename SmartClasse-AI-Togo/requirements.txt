# SmartClasse AI Togo — Python Dependencies
# Python 3.10+ | Compatible 3.10, 3.11, 3.12
# Install: pip install -r requirements.txt
#
# NOTE: PyTorch is NOT pinned here to avoid conflicts.
# Install separately BEFORE this file:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# Or for GPU:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118

# ── Core Framework ──────────────────────────────────────────────────────────
fastapi>=0.104.0,<1.0.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.1.0

# ── LLM — Gemma 4 via Ollama (local, offline) ──────────────────────────────
ollama>=0.3.0

# ── Audio Processing ─────────────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.11.0
soundfile>=0.12.0
pydub>=0.25.0
noisereduce>=2.0.1
librosa>=0.10.0
# webrtcvad: use prebuilt wheels package for Windows/Mac compatibility
webrtcvad-wheels>=2.0.10
imageio-ffmpeg>=0.4.8
langdetect>=1.0.9

# ── Text-to-Speech (offline) ─────────────────────────────────────────────────
pyttsx3>=2.90

# ── Speech-to-Text (offline Whisper) ─────────────────────────────────────────
# openai-whisper requires torch — install torch first (see note above), then:
openai-whisper>=20231117

# ── Database & Storage ───────────────────────────────────────────────────────
sqlalchemy>=2.0.0
cryptography>=41.0.0

# ── Web & Utilities ──────────────────────────────────────────────────────────
python-multipart>=0.0.6
requests>=2.31.0
python-dotenv>=1.0.0
loguru>=0.7.0

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=7.4.0
httpx>=0.25.0
